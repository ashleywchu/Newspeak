<%= form_tag({action: "create", controller: "subscriptions"})%>
<%= hidden_field_tag(:author_id, @user.id) %>
<%= hidden_field_tag(:reader_id, current_user.id) %>
<%= submit_tag "Subscribe", :class => "btn btn-default" %>