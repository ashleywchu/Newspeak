<h1><%= @user.name %>'s (<%= @current_user.reputation_for(:votes).to_i%>) News Feed</h1>
<% @user.subscriptions.each do |subscription| %>
<% if subscription.author %>
<%= subscription.author.name %>
(<%= subscription.author.reputation_for(:votes).to_i%>)
<br>
<% end %>
<% end %>